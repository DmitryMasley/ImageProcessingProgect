#ifndef IMAGEITEMDELEGATE_H
#define IMAGEITEMDELEGATE_H

#include <QWidget>

class ImageItemDelegate : public QWidget
{
    Q_OBJECT
public:
    explicit ImageItemDelegate(QWidget *parent = 0);
    ~ImageItemDelegate();

signals:

public slots:
};

#endif // IMAGEITEMDELEGATE_H
